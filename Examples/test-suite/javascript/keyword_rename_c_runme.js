var keyword_rename_c = require("keyword_rename_c");
/* await */(keyword_rename_c._instanceof(1));
/* await */(keyword_rename_c._finally(1));
/* await */(keyword_rename_c._yield(0));
/* await */(keyword_rename_c.yield = 0);
