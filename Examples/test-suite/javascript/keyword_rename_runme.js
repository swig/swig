var keyword_rename = require("keyword_rename");
/* await */(keyword_rename._instanceof(1));
/* await */(keyword_rename._finally(1));
/* await */(keyword_rename._yield(0));
/* await */(keyword_rename.yield = 0);
