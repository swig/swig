#include <stdio.h>

#include "example_proxy.h"

int main(int argc, char **argv) {
  int a = 35;
  int b = 15;
  printf("Foo is %f\n", Foo);
  printf("GCD(%d, %d)=%d\n", a, b, gcd(a, b));
  return 0;
}

